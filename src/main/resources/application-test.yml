web-socket-poc:
  url: "ws://localhost:9001/ws-poc"
  publish:
    heartbeat: "/app/heartbeat"
    batch-run: "/app/batch-run"
  topics:
    batch-run: "/topic/batch-run"
  scheduled:
    heartbeat:
      enabled: true
      cron: '0 0 12 25 12 MON'